<div class="container">
    <h2 class="h2 text-white text-center"><%=Proyecto.find(params[:proyecto_id]).nombre%></h2>
    <br>
    <div class="container">
        <div class="row justify-content-md-center align-items-center">
            <div class="col-md-10 align-item-center">
            <div class="card text-center">
                <div class="card-header">
                    <h3>Lanzamiento  : :  Metas | Editar Meta</h3>
                </div>
                <div class="card-body bg-aqua justify-content-center">
                <% @meta.each do |meta| %>
                    <form method="POST" action="edit/<%= meta.id %>" accept-charset="UTF-8" role="form" id="actualizarmeta" name="actualizarmeta" enctype="multipart/form-data"> 
                        <!-- Protección CSRF -->
                        <%= token_tag nil %>
                        <div class="row">
                            <div class="col-md-2">
                                <label class="text-white">Descripción</label>
                            </div>
                            <div class="col-md-10 input-group form-group">
                                <textarea type="text" class="form-control" placeholder="Descripción" id="descripcion" name="descripcion" required><%= meta.descripcion %></textarea>
                            </div>
                        </div>
                        <div class="row">
                            <div class="col-md-2">
                                <label class="text-white">Plazo</label>
                            </div>
                            <div class="col-md-10 input-group form-group">
                                <select type="select" class="form-control" name="plazo" id="plazo">
                                    <option value="">Seleccione una Opción</option>
                                    <% if meta.plazo == 'Corto' %>
                                    <option value="Corto" selected>Corto</option>
                                    <option value="Mediano">Mediano</option>
                                    <option value="Largo">Largo</option>
                                    <% elsif meta.plazo == 'Mediano'%>
                                    <option value="Corto">Corto</option>
                                    <option value="Mediano" selected>Mediano</option>
                                    <option value="Largo">Largo</option>
                                    <% else %>
                                    <option value="Corto">Corto</option>
                                    <option value="Mediano">Mediano</option>
                                    <option value="Largo" selected>Largo</option>
                                    <% end %>
                                </select>
                            </div>
                        </div>
                        <div class="form-group d-inline-flex mt-3">
                            <button type="submit" class="btn btn-light">Guardar</button>
                        </div>
                        <div class="form-group d-inline-flex mt-3">
                            <%= link_to 'Cancelar', url_for(:back), class: "btn btn-warning" %>
                        </div>
                    </form>
                    </div>
                <% end %>
                </div>
            </div>
        </div>
        </div>
    </div>
</div>