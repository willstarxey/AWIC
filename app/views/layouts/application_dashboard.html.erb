<!DOCTYPE html>
<html>
  <head>
    <title>Aplicación Web de Integración Continua - TSP Pruebas</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body class="container-fluid">
    <div class="row full-size">
      <div class="col-md-2 bg-aqua px-0 justify-content-center">
        <% if current_user.nil? %>
		      <%= link_to root_path do %>
            <%= image_tag("AWIC-TSP.png", :alt => "AWIC-TSP", :width => '100%')%>
          <% end %>
	      <% else %>
		      <%= link_to dashboard_index_path do %>
            <%= image_tag("AWIC-TSP.png", :alt => "AWIC-TSP", :width => '100%')%>
          <% end %>
	      <% end %>
        <% if current_user.admin? %>
	        <%= link_to "Usuarios", users_index_path, class: 'btn btn-lg btn-block btn-aqua' %>
          <%= link_to "Proyectos", proyectos_index_path, class: 'btn btn-lg btn-block btn-aqua' %>
        <% end %>
        <% if !current_user.admin? && !current_user.proyectos.nil?%>
          <div class="dropdown">
            <button class="btn btn-lg btn-block btn-aqua dropdown-toggle" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              <strong>Proyectos</strong>
            </button>
            <div class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdownMenuButton">
              <% current_user.proyectos.each do |proyecto| %>
                <%= link_to proyecto.nombre, dashboard_index_path(:proyecto_id => proyecto.id), class: 'dropdown-item' %>
              <% end %>
              <% if current_user.proyectos.empty? %>
                <a href="#" class="dropdown-item">No esta asociado a proyectos</a>
              <% end %>
            </div>
          </div>
          <% if !params[:proyecto_id].nil? %>
          <!--Menú basado en la selección de proyecto-->
            <hr class="my-1" color="#FFFFFF">
            <!--LANZAMIENTO-->
            <div class="dropdown">
              <button class="btn btn-lg btn-block btn-aqua dropdown-toggle" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                Lanzamiento
              </button>
              <div class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdownMenuButton">
                <% if current_user.role_id == 2%>
                  <%= link_to "Personal", lanzamiento_personal_index_path(:proyecto_id => params[:proyecto_id]), class: 'dropdown-item' %>
                <% end %>
                  <%= link_to "Metas", lanzamiento_metas_index_path(:proyecto_id => params[:proyecto_id]), class: 'dropdown-item' %>
              </div>
            </div>
            <!--ESTRATEGIA-->
            <div class="dropdown">
              <button class="btn btn-lg btn-block btn-aqua dropdown-toggle" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                Estrategia
              </button>
              <div class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdownMenuButton">
                <%= link_to "Diseños", estrategia_disenos_index_path(:proyecto_id => params[:proyecto_id]), class: 'dropdown-item' %>
                <%= link_to "Criterios", estrategia_criterios_index_path(:proyecto_id => params[:proyecto_id]), class: 'dropdown-item' %>
              </div>
            </div>
            <!--PLANEACIÓN-->
            <div class="dropdown">
              <button class="btn btn-lg btn-block btn-aqua dropdown-toggle" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                Planeación
              </button>
              <div class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdownMenuButton">
                <%= link_to "Planes de Calidad", planeacion_planes_calidad_index_path(:proyecto_id => params[:proyecto_id]), class: 'dropdown-item' %>
              </div>
            </div>
            <!--REQUERIMIENTOS-->
            <div class="dropdown">
              <button class="btn btn-lg btn-block btn-aqua dropdown-toggle" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                Requerimientos
              </button>
              <div class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdownMenuButton">
                <%= link_to "Requerimientos", requerimientos_requerimientos_index_path(:proyecto_id => params[:proyecto_id]), class: 'dropdown-item' %>
                <!--<a href="#" class="dropdown-item">Tareas</a>-->
              </div>
            </div>
            <!--DISEÑO-->
            <div class="dropdown">
              <button class="btn btn-lg btn-block btn-aqua dropdown-toggle" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                Diseño
              </button>
              <div class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdownMenuButton">
                <a href="#" class="dropdown-item">Estructuras</a>
                <a href="#" class="dropdown-item">Planes de Pruebas</a>
                <a href="#" class="dropdown-item">Estándares</a>
                <a href="#" class="dropdown-item">Reúsos</a>
              </div>
            </div>
            <!--IMPLEMENTACIÓN-->
            <div class="dropdown">
              <button class="btn btn-lg btn-block btn-aqua dropdown-toggle" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                Implementación
              </button>
              <div class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdownMenuButton">
                <a href="#" class="dropdown-item">Estructuras</a>
                <a href="#" class="dropdown-item">Planes de Pruebas</a>
                <a href="#" class="dropdown-item">Estándares</a>
              </div>
            </div>
            <!--PRUEBAS-->
            <div class="dropdown">
              <button class="btn btn-lg btn-block btn-aqua dropdown-toggle" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                Pruebas
              </button>
              <div class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdownMenuButton">
                <a href="#" class="dropdown-item">Estructuras</a>
                <a href="#" class="dropdown-item">Planes de Pruebas</a>
                <a href="#" class="dropdown-item">Estándares</a>
                <a href="#" class="dropdown-item">Reúsos</a>
              </div>
            </div>
            <!--POSTMORTEM-->
            <div class="dropdown">
              <button class="btn btn-lg btn-block btn-aqua dropdown-toggle" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                Postmortem
              </button>
              <div class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdownMenuButton">
                <a href="#" class="dropdown-item">Estructuras</a>
                <a href="#" class="dropdown-item">Planes de Pruebas</a>
                <a href="#" class="dropdown-item">Estándares</a>
              </div>
            </div>
          <% end %>
        <% end %>
      </div>
      <div class="col-md-10 p-5 bg-image-main">
          <div class="my-5">
          <!-- Mensajes -->
            <% flash.each do |type, msg| %>
              <% if type == "notice"
                    type = "success"
                  end 
              %>
              <div class="alert alert-<%= type %>" id="<%= type %>">
                <%= msg %>
              </div>
            <% end %>
            <%= yield %>
          </div>
      </div>
    </div>
    <footer class="row bg-grey-alt py-3">
      <div class="col-md-2 text-center justify-content-center">
        <% if !current_user.nil? %>
	        <%= link_to destroy_user_session_path, class: 'btn btn-aqua' , method: :delete do %>Cerrar Sesión<% end %>
        <% end %>
      </div>
    </footer>
  </body>
</html>